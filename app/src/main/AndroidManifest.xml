<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fanfics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fanfics"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.CompositionDetails" android:configChanges="uiMode" android:label="Composition"/>
        <activity android:name=".activity.OnboardingActivity" android:configChanges="uiMode" android:label="Onboarding"/>
        <activity android:name=".activity.SignupActivity" android:configChanges="uiMode" android:label="Signup"/>
        <activity android:name=".activity.MainActivity" android:configChanges="uiMode"/>
        <activity
            android:name=".activity.LoginActivity"
            android:configChanges="uiMode"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>